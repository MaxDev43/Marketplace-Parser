# Marketplace Parser

**Парсер товаров с маркетплейсов Ozon и Wildberries**
Автоматизированный инструмент для сбора карточек товаров с Ozon и Wildberries. Собирает название, цену, полное описание и характеристики.
Проект ориентирован на стабильность и удобство использования в условиях активной защиты маркетплейсов от автоматизации.  
На данный момент разработка завершена - проект находится в режиме **EOL (End-Of-Life)** и больше не будет активно развиваться.

## Содержание
- [Технологии](#технологии)
- [Поддержка](#поддержка)
- [Возможности](#возможности)
- [Начало работы](#начало-работы)
- [Особенности](#особенности)
- [Зачем нужен проект](#зачем-нужен-проект)
- [Структура проекта](#структура-проекта)
- [Использование](#использование)

## Технологии
- **Python 3.10+** (рекомендуется 3.13+)
- **Selenium**
- **undetected-chromedriver**
- **Google Chrome**

## Поддержка
- **Браузер**: Только Google Chrome (другие браузеры не поддерживаются из-за срабатывания Anti-Bot систем).
- **Операционная система**: Windows (поддержка Linux отсутствует из-за проблем с Chrome на моём дистрибутиве, но реализация возможна).

## Возможности
- Сбор товаров по ссылке на поисковую выдачу
- Полное извлечение описания (с раскрытием «Читать полностью»)
- Парсинг всех характеристик товара
- Обход антибота
- Поддержка Ozon и Wildberries
- Сохранение результатов в удобный JSON-файл
- Ведение подробных логов

## Начало работы

### Требования
- Python 3.10 или выше
- Установленный браузер Google Chrome
- Git

### Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/Max02117/Marketplace-Parser.git
cd Marketplace-Parser
```

2. Создайте виртуальное окружение и установите зависимости:
```bash
python -m venv venv
venv\Scripts\activate        # Windows
pip install -r requirements.txt
```

3. При необходимости измените глобальные настройки в файле `run.py`, например, `TARGET_PRODUCT_COUNT`, который отвечает за количество товаров, обрабатываемых по ссылке (по умолчанию 30).

### Создание удобного запуска (ярлык)

**Вариант 1 - Через .bat файл (рекомендуется):**
Создайте файл `start.bat` со следующим содержимым:
```bat
@echo off
cd /d <путь к корню проекта>
call venv\Scripts\activate
python run.py
pause
```
(замените путь на свой без '<>')

**Вариант 2 - Через ярлык Windows:**
- Создайте ярлык → В поле "Объект" укажите:
```
C:\Windows\System32\cmd.exe /k "<путь к activate.bat> && python <путь к run.py>"
```
(замените путь на свой без '<>')

### Использование

1. Скопируйте ссылку с поисковой страницы Ozon или Wildberries (с уже выбранными фильтрами).
2. Запустите основной файл:
```bash
python run.py
```
3. Вставьте скопированную ссылку. Программа автоматически определит маркетплейс и начнёт работу.

## Особенности
- Не рекомендуется использовать VPN - маркетплейсы часто блокируют такие подключения.
- На Wildberries антибот срабатывает значительно реже, поэтому парсер работает быстрее.
- Для увеличения скорости можно уменьшить таймауты, но это повышает риск срабатывания защиты и понижает стабильность.
- Можно запускать парсеры напрямую: `python parsers/ozon.py` или `python parsers/wildberries.py`.

## Зачем нужен проект
Мне была нужна программа, которая быстро собирает информацию по большому количеству товаров.  
Собранные данные удобно отправлять нейросети для анализа и выбора лучшего товара. Это значительно ускоряет и упрощает процесс сравнения.

## Структура проекта
```
Marketplace-Parser/
├── parsers/
│   ├── ozon.py              # парсер Ozon
│   └── wildberries.py       # парсер Wildberries
├── output/                  # результаты парсинга (.json)
├── logs/                    # логи работы
├── run.py                   # основной файл запуска
├── requirements.txt         # зависимости
├── README.md
└── LICENSE
```